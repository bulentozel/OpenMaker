This is a Wikipedia scraper. 
In order to run the scraper:
* Install Scrapy: https://doc.scrapy.org/en/latest/intro/install.html
* Go to the folder where you can run the crawler:
* Run the following command:

